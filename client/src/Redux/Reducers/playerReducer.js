

const initState = {userId: 0, turn: true, HP: 100 };

const playerReducer = (state = initState, action) => {
  const { type, payload } = action;

  switch (type) {
    case

    default:
      return state;
  }
};

export default playerReducer;
